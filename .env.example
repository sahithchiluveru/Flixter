# Copy to .env and fill in from your Supabase project (Project Settings -> API).
# These are bundled into the extension; only the anon key is safe to ship.
# The service_role key must NEVER be put here or in the extension.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
